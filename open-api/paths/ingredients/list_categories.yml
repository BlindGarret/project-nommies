summary: List all ingredient categories
operationId: listIngredientCategories
tags:
  - ingredients
parameters:
  - $ref: ../../parameters/default/offset_param.yml
  - $ref: ../../parameters/default/limit_param.yml
  - $ref: ../../parameters/default/sort_param.yml
responses:
  "200":
    description: Ingredient category response object containing list of categories
    schema:
      type: object
      properties:
        next:
          type: string
          description: Url to next page. Empty if no additional pages.
        recipes:
          type: array
          items:
            $ref: ../../definitions/ingredients/category.yml
  default:
    description: unexpected error
    schema:
      $ref: ../../definitions/default/error.yml